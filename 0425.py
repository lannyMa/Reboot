###exe1#### dict1={'name':'ww','age':12}# dict2={}# for key in dict1:#     dict2[key]=dict1[key]# print dict2# d1={'name':'pc','age':22}# d2={}# d2=d1# d1['name']='ww'# print d2###exe2#### read_me = '''# first of all, i want make it clear that i can not claim understanding this holy book  in just a few weeks, and i would not dare comment on this sacred book, in addition, i don't think i can give you a full picture of the holy bible in just few words.i can not preach anything here. what i want to do here is to express my understanding by sharing two events described in this book. the fist story i want to share is abandoned tower of babel.according to the bibel,people use the same language to communicate with each other in ancient times.with the soaring vanity,they decided to build a heaven-reaching tower to show off their achievement, god knows, he change the human language into different kinds and make it difficult for people to communicate with each other,hence the failure of building tower of  babel.this story tells people,never do things in selfishness, but make a life out of enternal glory.the other story,before jesus christ was crucified,he said, father,forgive them, for they know not what they do. with great love, he shouldered all the sins of  people. what can we learn from this story?we live in this world thanks to the love of god, for this reanson, we should make our lives glorious to honor our god.finally,i want to sum up by saying that only if we put our lives in the eternal love of god,can we live a perfect life, and  what you appealed is what god expected!# '''# init_list=list(read_me)# init_dict={}# for char in init_list:#     init_dict[char]=init_list.count(char)# print init_dict# d={}# for s in list(read_me):#     if s==' '###exe3#### keys_list=['name','age']# default_value=None# dct={}# for key in keys_list:#     if key not in dct:#         dct[key]=default_value# print dct###exe4#### dict1={'name':'ww','age':12}# for k in dict1:#     print k###exe5#### dict1={'name':'ww'}# dict2={'name':'wd','age':'27'}# for key in dict2:#     dict1[key]=dict2[key]# print dict1###exe6#####way1init_dict={'waihao':'pc','name':'pc','age':12,'job':'IT'}new_dict={}for init_key in init_dict:    if init_dict[init_key] in new_dict:        if type(new_dict[init_dict[init_key]])==type([]):            new_dict[init_dict[init_key]].append(init_key)        else:            tmp_list=[]            tmp_list.append(init_key)            tmp_list.append(new_dict[init_dict[init_key]])        new_dict[init_dict[init_key]]=tmp_list    else:        new_dict[init_dict[init_key]]=init_keyprint new_dict##way2# init_dict={'waihao':'pc','name':'pc','age':12,'job':'IT'}# new_key=init_dict.values()# key_list=[]# new_dict={}# for nk in new_key:#     tmp_count=new_key.count(nk)#     if tmp_count>1:#         if nk in key_list:#             continue#         else:#             key_list.append(nk)#     else:#         key_list.append(nk)# new_dict.fromkeys(key_list)## for key in key_list:#     tmp_list=[]#     for t in init_dict.items():#         if t[1]==key:#             tmp_list.append(t[0])#     if len(tmp_list)==1:#         new_dict[key]=tmp_list[0]#     else:#         new_dict[key]=tmp_list# print new_dict##way zhangchen# old_dict = {'waihao':'pc','name':'pc','age':12,12:12,'zhangchen':12,'job':'IT'}# new_dict = {}# for key,value in old_dict.items():#     if value in new_dict:#         if type(new_dict[value]) == list:#             new_dict[value] = new_dict[value] + [key]#         else:#             new_dict[value] = [new_dict[value]] + [key]#     else:#         new_dict[value] = key# print new_dict